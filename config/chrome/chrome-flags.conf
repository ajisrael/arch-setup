# Chrome flags - read by /usr/bin/google-chrome-stable (AUR wrapper).
# Intentionally empty: Chrome's scale now comes from Hyprland's monitor scale
# (set to 1 in config/hypr/hyprland.lua). --force-device-scale-factor is a
# no-op on Wayland (not supported by Chromium), so there is nothing to set here
# - Chrome follows the compositor. Add future flags below if needed.
